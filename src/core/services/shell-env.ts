// Re-export from shared — shell-env is a standalone utility with no core dependencies.
// Canonical location: src/shared/shell-env.ts
export { initShellEnv, getUserShellPath, getShellEnv } from '../../shared/shell-env';
