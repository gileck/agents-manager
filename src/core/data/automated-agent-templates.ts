import type { AutomatedAgentTemplate } from '../../shared/types';

export const AUTOMATED_AGENT_TEMPLATES: AutomatedAgentTemplate[] = [
  {
    id: 'project-manager',
    name: 'Project Manager',
    description: 'Reviews all open tasks daily and provides status updates, identifies blockers, and suggests next actions.',
    promptInstructions: [
      'You are a project manager agent. Review the current state of the project.',
      '',
      'Tasks:',
      '1. Review all open tasks and their current statuses.',
      '2. Identify any tasks that appear to be blocked or stalled.',
      '3. Check for tasks with missing descriptions or unclear requirements.',
      '4. Suggest priority adjustments if needed.',
      '5. Provide a concise project status summary.',
      '',
      'Output a structured report with sections: Summary, Blocked Tasks, Recommendations.',
    ].join('\n'),
    defaultSchedule: { type: 'daily-at', value: '09:00' },
    defaultCapabilities: { canCreateTasks: true, canModifyTasks: false, readOnly: false, dryRun: false, maxActions: 50 },
    defaultMaxRunDurationMs: 300000,
  },
  {
    id: 'tech-debt-scanner',
    name: 'Tech Debt Scanner',
    description: 'Scans the codebase weekly for tech debt patterns, outdated dependencies, and code quality issues.',
    promptInstructions: [
      'You are a tech debt scanner. Analyze the codebase for technical debt.',
      '',
      'Tasks:',
      '1. Check for TODO/FIXME/HACK comments in the codebase.',
      '2. Look for outdated dependencies in package.json.',
      '3. Identify large files or functions that might need refactoring.',
      '4. Check for duplicated code patterns.',
      '5. Review test coverage gaps.',
      '',
      'Output a prioritized list of tech debt items with severity and recommended actions.',
    ].join('\n'),
    defaultSchedule: { type: 'interval', value: '604800000' }, // 7 days
    defaultCapabilities: { canCreateTasks: true, canModifyTasks: false, readOnly: false, dryRun: false, maxActions: 50 },
    defaultMaxRunDurationMs: 600000,
  },
  {
    id: 'pr-reviewer',
    name: 'PR Reviewer',
    description: 'Checks open pull requests hourly for code quality, tests, and potential issues.',
    promptInstructions: [
      'You are a PR reviewer agent. Review open pull requests in this project.',
      '',
      'Tasks:',
      '1. List all open branches and recent PRs.',
      '2. For each open PR, review the diff for code quality issues.',
      '3. Check that tests are included for new functionality.',
      '4. Look for potential security issues.',
      '5. Verify documentation is updated if needed.',
      '',
      'Output a review summary for each PR with: status, findings, and recommended actions.',
    ].join('\n'),
    defaultSchedule: { type: 'interval', value: '3600000' }, // 1 hour
    defaultCapabilities: { canCreateTasks: false, canModifyTasks: false, readOnly: true, dryRun: false, maxActions: 30 },
    defaultMaxRunDurationMs: 300000,
  },
  {
    id: 'status-reporter',
    name: 'Status Reporter',
    description: 'Generates a daily project status report with task progress, recent activity, and metrics.',
    promptInstructions: [
      'You are a status reporter agent. Generate a project status report.',
      '',
      'Tasks:',
      '1. Count tasks by status (open, in progress, done).',
      '2. List tasks completed in the last 24 hours.',
      '3. List tasks that changed status recently.',
      '4. Calculate overall project progress percentage.',
      '5. Note any upcoming deadlines or milestones.',
      '',
      'Output a concise status report suitable for team communication.',
    ].join('\n'),
    defaultSchedule: { type: 'daily-at', value: '08:00' },
    defaultCapabilities: { canCreateTasks: false, canModifyTasks: false, readOnly: true, dryRun: false, maxActions: 20 },
    defaultMaxRunDurationMs: 180000,
  },
  {
    id: 'stale-task-closer',
    name: 'Stale Task Closer',
    description: 'Identifies and closes tasks that have been inactive for an extended period.',
    promptInstructions: [
      'You are a stale task management agent. Identify tasks that may need to be closed.',
      '',
      'Tasks:',
      '1. Find tasks that have not been updated in over 30 days.',
      '2. Check if stale tasks are still relevant to the project.',
      '3. For clearly outdated tasks, mark them for closure.',
      '4. For ambiguous tasks, flag them for human review.',
      '',
      'Output a list of stale tasks with recommended actions (close, review, or keep).',
    ].join('\n'),
    defaultSchedule: { type: 'daily-at', value: '02:00' },
    defaultCapabilities: { canCreateTasks: false, canModifyTasks: true, readOnly: false, dryRun: false, maxActions: 20 },
    defaultMaxRunDurationMs: 300000,
  },
  {
    id: 'task-triage',
    name: 'Task Triage',
    description: 'Triages the next open task: classifies type, estimates size/complexity, and routes to the appropriate pipeline stage. Processes one task per run.',
    promptInstructions: [
      'You are a task triage agent. The "Task to Triage" section above contains the single task you must process.',
      'If it says "No untriaged open tasks found", report that and stop — there is nothing to do.',
      '',
      'For the given task:',
      '1. Briefly scan the codebase for any files or areas related to the task to validate relevance.',
      '2. Classify the task:',
      '   - type: bug | feature | chore | refactor | docs | test',
      '   - size: xs | sm | md | lg | xl',
      '   - complexity: low | medium | high',
      '3. Determine the routing target based on complexity and size:',
      '   - high complexity (any size) → designing',
      '   - medium complexity + lg/xl size → designing',
      '   - medium complexity + md size → planning',
      '   - medium complexity + sm/xs size → implementing',
      '   - low complexity (any size) → implementing',
      '   - If type is bug and the root cause is unclear or needs investigation → investigating',
      '   - If the description is too vague to assess → complexity=medium, route to planning',
      '',
      '4. Use the CLI to apply changes:',
      '   node <project-path>/bootstrap-cli.js task update <task-id> --type <type> --size <size> --complexity <complexity>',
      '   node <project-path>/bootstrap-cli.js task add-context <task-id> --type triage_summary --source triage-agent --summary "<one-line triage result>"',
      '   node <project-path>/bootstrap-cli.js task transition <task-id> <target-status>',
      '',
      '5. Edge cases:',
      '   - If the task appears stale or no longer relevant: add a triage_summary noting "stale — may no longer be relevant". Do NOT close or transition the task.',
      '   - If the transition fails: add a triage_summary noting the failed transition and the reason.',
    ].join('\n'),
    defaultSchedule: { type: 'manual', value: '' },
    defaultCapabilities: { canCreateTasks: false, canModifyTasks: true, readOnly: false, dryRun: false, maxActions: 50 },
    defaultMaxRunDurationMs: 300000,
  },
];
